# Sample workbook

Run `pnpm db:push && pnpm db:seed`, sign in, and select **Export Excel** from the seeded Autumn Engineering Interviews event to generate a representative workbook. It includes all required sheets and seeded submission rows. The workbook is generated rather than committed so no mutable reporting artifact is mistaken for the source of truth.
