import { redirect } from "next/navigation"; import { setAdminSession } from "@/lib/admin";
async function login(formData:FormData){"use server"; if(formData.get("email")===process.env.ADMIN_EMAIL && formData.get("password")===process.env.ADMIN_PASSWORD){await setAdminSession();redirect("/admin")} redirect("/admin/login?error=1")}
export default async function Login({searchParams}:{searchParams:Promise<{error?:string}>}){const error=(await searchParams).error; return <main style={{maxWidth:460}}><div className="card"><h1>Admin sign in</h1><p className="muted">Development-only access gate. Replace with SSO in production.</p>{error&&<p className="warning">Invalid email or password.</p>}<form action={login}><label htmlFor="email">Email</label><input id="email" name="email" type="email" required/><label htmlFor="password">Password</label><input id="password" name="password" type="password" required/><p><button>Sign in</button></p></form></div></main>}
